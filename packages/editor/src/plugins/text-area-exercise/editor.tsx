import type { TextAreaExerciseProps } from '.'
import { TextAreaExerciseRenderer } from './renderer'

export function TextAreaExerciseEditor({ id: _id }: TextAreaExerciseProps) {
  return <TextAreaExerciseRenderer />
}
